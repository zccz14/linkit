import type { RedirectSessionInput } from 'auth-mini/sdk/browser';
export type AuthMiniRedirectCallback = {
    state: string;
    tokens: RedirectSessionInput;
    cleanUrl: string;
};
export declare class AuthMiniCallbackError extends Error {
    readonly cleanUrl: string;
    constructor(message: string, cleanUrl: string);
}
export declare function getAuthMiniLoginStateKey(authMiniBaseUrl: string): string;
export declare function getAuthMiniSecurityUrl(authMiniBaseUrl: string): string;
export declare function resolveAuthMiniAudience(audience: string | undefined, hostname?: string): string | undefined;
export declare function getAuthMiniLoginUrl(input: {
    authMiniBaseUrl: string;
    audience?: string;
    callbackUrl: string;
    state: string;
}): string;
export declare function readAuthMiniRedirectCallback(href: string): AuthMiniRedirectCallback | null;
