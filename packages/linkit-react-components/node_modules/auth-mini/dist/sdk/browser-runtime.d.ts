import type { AuthMiniInternal, FetchLike, InternalSdkDeps } from './types.js';
export type BrowserLockManager = {
    request<T>(name: string, options: {
        mode: 'exclusive';
    }, callback: () => Promise<T>): Promise<T>;
};
export type BrowserSdkFactoryOptions = {
    fetch?: FetchLike;
    lockManager?: BrowserLockManager;
    now?: () => number;
    storage?: Storage;
};
export declare function createAuthMiniInternal(input: InternalSdkDeps): AuthMiniInternal;
export declare function createBrowserSdkInternal(baseUrl: string, options?: BrowserSdkFactoryOptions): AuthMiniInternal;
export declare function renderBrowserRuntimeSource(): string;
