import type { HttpClient } from './http.js';
import type { EmailStartInput, EmailStartResponse, EmailVerifyInput, SessionResult } from './types.js';
type SessionController = {
    acceptSessionResponse(response: unknown): Promise<SessionResult>;
};
export declare function createEmailModule(input: {
    http: HttpClient;
    session: SessionController;
}): {
    start(payload: EmailStartInput): Promise<EmailStartResponse>;
    verify(payload: EmailVerifyInput): Promise<SessionResult>;
};
export {};
