import { type KeyObject } from 'node:crypto';
import type { HttpClient } from './http.js';
type DeviceAuthSession = {
    acceptSessionResponse(response: unknown): Promise<unknown>;
};
export declare function deriveDevicePrivateKey(privateKey: string): KeyObject;
export declare function deriveLegacyDevicePrivateKey(privateKeySeed: string): KeyObject;
export declare function deriveDevicePublicKey(privateKey: KeyObject): string;
export declare function authenticateDevice(input: {
    http: HttpClient;
    privateKey: KeyObject;
    session: DeviceAuthSession;
}): Promise<void>;
export {};
